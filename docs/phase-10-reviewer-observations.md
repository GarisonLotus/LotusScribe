# Reviewer Observations — LotusScribe (Phase 10)

Numbered forward-looking items. id | item | status | sub-phase first raised.

| ID | Item | Status | Raised |
|----|------|--------|--------|
| R10A-1 | `HotkeyPicker` menu row (line 90) still prints the literal `"Custom"` for `.custom` options, so the new spelled label surfaces only on function-key rows + the onboarding copy/HUD — not inside the Settings picker dropdown for a custom chord. If a future request wants the spelling visible there too, that call site needs its own change. | Open | 10A |
| R10A-2 | `keyName(for:)` does a linear `.first(where:)` scan of both maps per call. Fine for label rendering; revisit only if `spelledLabel` ever lands in a per-frame path (e.g. live HUD redraw). | Open | 10A |
| R10B | Clean.Renumber consistent (stepContent/progressDots 0..<4/navBar); kickers STEP 1-4 OF 4 correct; case 2 Continue always enabled, case 3 Finish keeps D67 gate + defaultAction; setupStep ScrollView+LotusCard, theme fonts only (13pt). Build green. | Clean | 10B |
| R10E1 | Clean. Additive/nil-safe: every fire is `onOutcome?(…)` appended AFTER the existing terminal statement at each point (pill.hide/update/insert) — no reordering, loop identical when nil (asserted `onOutcome == nil`). Branch points verified against current `stopRecording()`: `.tooShort` in `guard hasUsableAudio` else pre-Task; `.empty` past D23 stale guard; `.inserted` past D43 post-cleanup re-check; `.failed` INSIDE `capturedGeneration==generation` catch guard. STALE-DROP INVARIANT HOLDS — both stale-return sites (D23 pre-cleanup, D43 post-cleanup) fire NOTHING. `shouldShowSetupHint` pure/nonisolated-static, empty/failed→true else; 5 genuine asserts incl nil→false. Wiring: AppDelegate posts `.lotusDictationOutcome` userInfo `["outcome": rawValue]`, Name beside `.lotusHotkeyChanged`; DictationController stays NotificationCenter-free (callback only). 4 files staged; comments cite D96/D97/D14. Re-ran make test: 263/24 green. | Clean | 10E1 |
| R10C | Clean. `var … = nil` on suggested models is CONFIRMED sound — a defaulted `let` is dropped from the synthesized memberwise init, so `var` is required for the named featured entries to pass values (init source-compat kept; `.all`/tests compile, 258/24 green). Speaches→whisper-large-v3, Ollama→llama3.2:3b, vLLM nil. `apply(to:)` unchanged, never touches models (asserted L102-108). Controller owns its own draft; `commitSetup()`=`draft.save()` is the sole write, `reload()` on show — D26 untouched, no per-keystroke store write.Continue commits THEN advances (ungated). `applyRecommended` prefill test asserts all 4 fields.Local field builders are byte-exact mirrors of SettingsForm's private idioms (LotusTheme only; `.orange` hint mirrors Settings). ≥12pt.Only the 4 files staged. | Clean | 10C |
| R10E2 | Clean (CODE review; runtime self-insertion is HUMAN-verified, not checked here). Focus: `@FocusState tryItFocused` set true only via `.onAppear` on `tryItStep`, which is instantiated solely at `switch stepIndex … default: tryItStep` (case 3) — no global/always-on focus; the `.focused` binding target exists only inside tryItStep so it can't steal input on steps 0-2 or fight HotkeyPicker's own field.Hint decode: `.onReceive(.lotusDictationOutcome)` reads `userInfo["outcome"] as? String` → `DictationOutcome(rawValue:)` → `lastOutcome`; render `if shouldShowSetupHint(for: lastOutcome)` sits INSIDE tryItStep so the `stepIndex==3` gate is satisfied STRUCTURALLY (switch) rather than literally — functionally equivalent to spec's `stepIndex==3 &&`; "Back to setup"→`stepIndex=2`. HUDPreview struct + `hudPreview` prop removed; only residual "HUDPreview" is prose in an L203 why-comment.No orphaned tokens: `lotusHUDFill` still used PillView.swift:27, `lotusAccentPink` used PillView.swift:84 + StatusItemController.swift:70.Surgical: only OnboardingView.swift; TextEditor styled with lotusMono(12)/lotusTextPrimary/lotusControlFill/lotusSurfaceBorder (no raw hex), text ≥11pt, no new animation (Reduce Motion moot). Comments cite D97/D98/D99.make build green. | Clean | 10E2 |
| R10E2-1 | Hint/focus gating is IMPLICIT via the `stepIndex` switch (tryItStep only exists at case 3), not the literal `stepIndex==3 &&` the spec/D97 wrote.Correct today.If tryItStep is ever reused outside case 3, the implicit gate silently breaks — cheap to harden with an explicit guard if that refactor lands.Non-blocking. | Open | 10E2 |
