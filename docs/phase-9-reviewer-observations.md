# Reviewer Observations — LotusScribe (Phase 9)

Numbered forward-looking items. Newest at bottom; drop resolved rows.

| ID | Sub-phase | Item | Status |
|----|-----------|------|--------|
| R9A-1 | 9A | Reviewed clean: `handleCombo` untouched (diff shows no edits; D30 pair-balance preserved), F1–F12 keycodes match HIToolbox kVK, `parse`/`resolved`/`HotkeyOption` logic + `persisted!` unwrap all correct. APPROVE. | Resolved |
| R9A-2 | 9A | Minor test nit: bare-digit rejection wasn't explicitly asserted. Fixed — added `"5"`/`"9"` to `parseRejectsGarbage` arguments. | Resolved |
| R9C-1 | 9C | Reviewed clean. Dictation wiring preserved: `HotkeyController { dictation.handle($0) }` → invoked via `MainActor.assumeIsolated { self?.onAction(action) }` in the tap closure (main run loop), identical main-thread, single-dispatch behavior to the old inline `assumeIsolated { dictation.handle(action) }`. `rebind()` stops the old monitor (EventTapMonitor.stop() disables tap + removes run-loop source) before building/starting a new one → no double-tap/leak; all on main, no race. Observer `[weak self]` + `deinit` removal is leak-safe for both app-lifetime and test-created controllers. R35 smoke + write-through tests assert construction/start and BOTH store write and notification post. APPROVE. | Resolved |
| R9C-2 | 9C | Non-blocking nit: `MainActor.assumeIsolated { self?.rebind() }` in the observer is belt-and-suspenders — `rebind()` is nonisolated and touches no MainActor state, so the wrapper isn't compiler-required (queue:.main already guarantees main-thread). Harmless; documents intent. Leave as-is. | Open |
| R9D-1 | 9D | Reviewed clean. `HotkeyPicker` correct: F1–F12 buttons `commit` (persist+post via `HotkeySetting.set`); `Custom…` only reveals the field (`option = .custom(customText)`, no commit → empty never persists); `onChange` live-commits only when `HotkeyChord.parse(text) != nil` (partial/invalid typing keeps the old chord); `customInvalid` hint gated on non-empty-and-unparseable; init seeds `option`/`customText` from `HotkeyOption.from(persisted:)`. Onboarding: `fnKeycap` fully removed (no dangling refs), `@State hotkeyLabel` seeds from store, `.onReceive(.lotusHotkeyChanged)` refreshes it, `HUDPreview(hotkeyLabel:)` chip uses it, F5/Dictation guidance present. Settings `hotkeyCard` inserted before `appearanceCard`, live write-through (no SettingsDraft/probe gate), matching `appearanceCard`. Reskin: LotusTheme tokens/components only, no raw hex; fonts ≥11pt (chevron is a 9pt SF Symbol glyph, not text); `.orange` warning matches existing PillView/SettingsForm convention. Comment density matches reskinned files. APPROVE. | Resolved |
| R9E-1 | 9E | Reviewed: core clean. `HotkeyCollision.warning(for:)` pure lookup correct for the specified cases (F5 → 2 links Siri+Keyboard; "fn"/"FN" → 1 link; F1–F4/F6–F12 and combos incl. "shift+f5" → nil, intentional). Orphan removal verified: `keyboardPane` gone from OnboardingView, `open(_:)` retained and still used by both permission rows (lines 104/120); Settings caption trimmed without breaking the card. Reskin rules met: `.lotusCaption` + `.orange` (matches existing `customInvalid` convention), `LotusButtonStyle(.ghost)`, `import AppKit` present for NSWorkspace. Warning keys off committed `option`, so partial custom typing never flashes it. Tests cover the mapping only, as intended. APPROVE (with two non-blocking edge gaps below). | Resolved |
| R9E-2 | 9E | Edge gap: typing `f5` (or `F5`) in the *custom* field commits `.custom("f5")`, which `parse` resolves to the same physical F5 (`combo(96, [])`) — but `warning(for:)` matches syntactically on `.functionKey(5)`, so no collision warning shows in-session. After relaunch, `HotkeyOption.from(persisted: "f5")` normalizes it to `.functionKey(5)` and the warning appears — inconsistent. Suggested fix: match on the resolved chord (`option.chord`): `.combo(96, [])` → F5 warning, `.fnHold` → fn warning. This preserves "shift+f5 is clean" (`.combo(96, .maskShift)` ≠ `.combo(96, [])`). | Resolved — fixed: warning(for:) now matches option.chord (combo(96,[]) / .fnHold); test spellingVariantsResolveToTheSameWarning covers it |
| R9E-3 | 9E | Edge gap (same root cause): whitespace-padded custom `" fn "` / `"fn "` — `HotkeyChord.parse` trims per-token so it commits as a working `.fnHold` chord, but `string.lowercased() == "fn"` fails on the untrimmed string → the fn/globe warning is silently dropped. The chord-based match in R9E-2 fixes this too; a cheaper patch is `string.trimmingCharacters(in: .whitespaces).lowercased() == "fn"`. | Resolved — same chord-based fix |
