# Architect Log — LotusScribe (Phase 10)

Locked decisions (compact; git log carries provenance). Newest at bottom.
Continues numbering from Phase 9 (…D88).

| ID | Date | Decision | Why | Sub-phase |
|----|------|----------|-----|-----------|
| D89 | 2026-07-06 | Hotkey label spelled as words joined by `" + "` → "Command + F5" (NOT glyphs "⌘F5"), from a PURE `HotkeyChord.spelledLabel` (reverse-looks-up the existing keycode maps, spells modifiers Control/Option/Shift/Command). `HotkeyOption.displayLabel` redefined to delegate; all 3 sites (HUD chip, picker menu, Try-it copy) inherit it | User's phrasing was "Command + F5"; must reflect the REAL resolved chord (D87), never hardcoded; no test references `displayLabel`, so redefining is safe | 10A |
| D90 | 2026-07-06 | Onboarding Setup persists via a buffered `SettingsDraft` (owned by `OnboardingWindowController`) committed by `draft.save()` on Continue — ungated (skippable), no D42 warm-up. Hotkey stays live write-through (D83). D26 ("Save is sole store-write in the **Settings window**") untouched — onboarding is a separate window/draft; mirrors Settings' own endpoint-buffered / hotkey-live split | Endpoints are buffered+probe-gated by nature in Settings; a per-keystroke live write of half-typed URLs to the shared store is wrong. Buffered draft REUSES `apply`, `endpointField`, `ConnectionProbe` seams and gives onboarding a coherent, D26-safe write path | 10C |
| D91 | 2026-07-06 | Suggested models live on `EndpointPreset` as new optional `suggestedSTTModel`/`suggestedLLMModel` (default nil → memberwise-init source-compatible). Speaches `whisper-large-v3`, Ollama `llama3.2:3b`, vLLM nil. `apply(to:)` UNCHANGED (D69 — never sets model fields). Entries named `static let speaches/ollama/vllm` so onboarding references the featured two | Keeps model+endpoint together and reusable; must not regress the Settings preset menu, which relies on `apply` not touching models | 10C |
| D92 | 2026-07-06 | Onboarding shows install cards for the featured servers ONLY (Speaches, Ollama); vLLM gets none (stays a Settings preset). Each card = short numbered steps + ONE copyable mono command + a "Full docs" link. Speaches: `docker run -p 8000:8000 ghcr.io/speaches-ai/speaches:latest`, docs speaches.ai. Ollama: `ollama pull llama3.2:3b`, docs ollama.com | Locked format (handoff §3) must fit 480×480; three servers is a wall. Commands realistic-not-code-verified per task rails | 10D |
| D93 | 2026-07-06 | Flow Welcome→Permissions→**Setup(NEW)**→Try it. `stepIndex` 0/1/2/3; `stepContent` adds `case 2: setupStep`; `progressDots` 3→4; `navBar` adds `case 2` (Back→1, Continue→3 always enabled); Try-it becomes `case 3` (Finish gated `.done`, D67). Kickers all → "STEP N OF 4" | Setup gate LOCKED skippable — Continue never blocks; Finish's all-green gate stays on Try it | 10B/10D |
| D94 | 2026-07-06 | Slice: 10A label clarity (ship first) → 10B step scaffold+renumber → 10C persistence+featured-prefill+fields → 10D install content+connection test. Each independently committable, 4-way gated | 10A is the quick win and touches only the label; 10B is pure UI scaffolding; 10C is data/persistence; 10D is content+probe | — |
| D95 | 2026-07-06 | **(a) ACCEPT** the local `labeledField`/`monoField`/`endpointField` mirror in `OnboardingView.swift`; do NOT widen SettingsForm's privates or extract a shared component now. D90's "REUSES … endpointField" is reconciled to mean **reuse the idiom**, not the symbol. **Tripwire:** extract to a shared LotusTheme/field component the moment a 3rd caller appears OR a field row gains real behavior (validation/probe wiring beyond the current advisory hint). No sub-phase added. **Also (Deviation 1):** D91's preset fields ship as `var …: String? = nil` (not `let`) — CONFIRMED not a shape change: a `let` with a default is *excluded* from Swift's synthesized memberwise init, so `.speaches`/`.ollama` could not set `suggested*Model:`; `var` is the mechanical necessity that keeps existing entries source-compatible while letting featured entries set the value. D91's intent (optional, default nil, `apply` untouched) is intact. | The "second caller → extract" heuristic is real but here it's outweighed: the actual DRY value (design tokens — `lotusMono`, `lotusControlFill`, `lotusSurfaceBorder`) is ALREADY centralized in LotusTheme, so the duplication is ~35 lines of stable, trivial view-builder glue over an already-shared layer. Extraction's cost is asymmetric — it forces touching the already-reviewed, 4-way-gated, reskinned SettingsForm (D26-sensitive) plus re-gating two consumers, against low divergence risk (any drift is cosmetic and caught by the reskin's human-at-screen checks). Surgical-change discipline + "don't refactor what isn't broken" win at n=2; the named tripwire honors the heuristic's spirit at n=3. | 10C |

| D96 | 2026-07-06 | **DictationController outcome seam.** Add `enum DictationOutcome: String { inserted, empty, failed, tooShort }` + `var onOutcome: ((DictationOutcome)->Void)?` — mirrors `onListeningChanged` exactly (optional, nil in headless tests, main-actor, additive: loop behavior identical when nil). Fires at the EXISTING `stopRecording()` branch points, CURRENT generation only: `.tooShort` at the `guard hasUsableAudio` else (pre-Task, synchronous); `.empty` at `guard !text.isEmpty` else (past D23 stale guard); `.inserted` after `inserter.insert(text)`+`pill.update(terminal)` (past D43 post-cleanup re-check); `.failed` INSIDE the transcription-catch `capturedGeneration==generation` guard. The two stale-drop returns fire nothing. Task-body fires are already on MainActor (it calls `pill.update` directly). | Purely observational seam; the precedent (`onListeningChanged`) is the exact shape the user asked to mirror. Reusing the already-generation-gated branch points means D23/D43 stale-drop logic is untouched — stale results never signal, exactly as they never touch the pill | 10E1 |
| D97 | 2026-07-06 | **Observation wiring = callback → NotificationCenter → SwiftUI `.onReceive`.** AppDelegate sets `dictation.onOutcome` (beside `onListeningChanged`) to post `.lotusDictationOutcome` (`userInfo:["outcome": rawValue]`). OnboardingView observes via `.onReceive(publisher(for: .lotusDictationOutcome))` into `@State lastOutcome`. Hint shown iff `stepIndex==3 && DictationController.shouldShowSetupHint(for:lastOutcome)` (PURE, D14: true iff `.empty`/`.failed`; `.inserted` clears). | Loosest of the three candidates: no window reference anywhere on the dictation side, and it REUSES the exact `.lotusHotkeyChanged` idiom OnboardingView already consumes. Keeping DictationController NotificationCenter-free (callback only) preserves "nil in headless tests." Lifecycle is automatic: SwiftUI tears the `.onReceive` down when the window closes, so a closed window never reacts — no manual set-on-show/clear-on-close needed. The permanently-wired AppDelegate callback is a harmless bare ping when no onboarding view is alive (mirrors `onListeningChanged` staying wired for the app's life) | 10E1 |
| D98 | 2026-07-06 | **Focus.** Try-it box is `@FocusState`-driven, set first responder when `stepIndex==3` (on appear / on change) so a synthesized Cmd-V lands in it. No LSUIElement obstacle: the onboarding window is already made key (`makeKeyAndOrderFront` + `NSApp.activate(ignoringOtherApps:)`) and the HotkeyPicker's custom field already accepts key input today, proving the key-window path works without a dock icon | Insertion probes the SYSTEM-WIDE focused element (`TextInserter`) — the box must be first responder and its window key or the real path can't be proven. After using the picker (which takes focus to record a chord) the user re-clicks the box; acceptable, flagged human-verify | 10E2 |
| D99 | 2026-07-06 | **Try-it layout.** KEEP `HotkeyPicker()`. REPLACE the decorative `HUDPreview` with the real focused editable box (LotusTheme mono, taller `monoField` idiom) + prompt "Hold \(hotkeyLabel) and speak — your words appear here." + inline failure hint ("No text? Check your servers." + "Back to setup" → `stepIndex=2`, shown on `.empty`/`.failed`). `HUDPreview` becomes orphaned (its only caller) → removed as own-change cleanup (CLAUDE.md §3). | The real `PillController` panel already shows live Listening feedback during dictation, so the decorative mini-HUD is now redundant; the real box is the honest artifact. Failure UX is the user-locked inline-hint-+-jump-to-Setup, reusing the existing advisory-hint reskin idiom | 10E2 |
| D100 | 2026-07-06 | **Slice + sequencing.** 10E = TWO sub-phases: **10E1** (outcome seam + AppDelegate notification wiring + pure `shouldShowSetupHint` + `DictationControllerTests`) — headless, low-risk, valuable under BOTH the authentic and the fallback path; **10E2** (focused box + focus mgmt + inline hint; HUDPreview removal), whose FIRST engineer step is the self-insertion DE-RISK spike — if it fails, STOP + surface, contingency = preview-sink fallback ONLY. **10E ships BEFORE 10D** (user-sequenced): Try-it must be provably live before install content lands. Each sub-phase independently committable + 4-way gated. | Risk is concentrated in the empirical self-insertion question; isolating it to 10E2's opening keeps 10E1 (the seam, needed either way) safe and mergeable. Two slices honor "independently committable"; the de-risk-first ordering prevents building the hint atop an unproven insertion path | — |

## Open questions

- Install command exactness: the Speaches `docker run` image tag and the Ollama
  steps are realistic-but-unverified (task rails say do not code-verify servers).
  Engineer/human should confirm against current Speaches/Ollama docs before 10D
  ships — not a blocker for 10A/10B.
- None block 10A: it is self-contained (label formatter + 3 call sites).
- **10E de-risk endpoint (needs a user ruling before 10E2's spike):** the
  featured localhost Speaches/Ollama return nothing unless the user runs them,
  so the ONLY way to verify the SUCCESS path (real text in the box) is to point
  the STT endpoint at a reachable server — the user's vLLM
  (`https://vllm.garison.com/v1/audio/transcriptions`, per MEMORY) is the
  practical target. Confirm the engineer/human may temporarily set that STT
  endpoint for the de-risk + human-verify. Not a code blocker for 10E1.
- **10E `.tooShort` copy (minor):** spec keeps the hint on `.empty`/`.failed`
  ONLY (`.tooShort` = tap-length press, a user action, not a server problem →
  no "check your servers"). If the user wants a gentler "hold a little longer"
  note on `.tooShort`, that's a one-line predicate/copy add — flag, don't block.
