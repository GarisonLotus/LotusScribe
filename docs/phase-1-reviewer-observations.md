# Reviewer observations — LotusScribe (Phase 1)

> Forward-looking items for Phase 1. Phase-0 archive:
> docs/phase-0-reviewer-observations.md (R1–R4; numbering continues here).

## Items

| id | first raised | item | status |
|----|--------------|------|--------|
| R3 | 0B (carried) | 0A link-smoke test: repoint at real behavior in Phase 1 | open |
| R4 | 0B (carried) | Legacy-keychain ACLs vs ad-hoc re-signing may break later-phase API-key reads; resolves with Q1/D12 | open |
| R5 | 1A | TCC-request guard (AppDelegate: `XCTestSessionIdentifier` env check around `CGRequestListenEventAccess()`): execution is clean — single call site, why-comment, marker empirically verified. But whether to request at launch at all vs. drop the call (user grants via System Settings) changes the TCC empirical record (spec §1A verify 3) and Phase-7 onboarding UX → SHAPE question, routed to orchestrator | ruled: keep launch request (D22) |
| R6 | 1A | LoC ceiling overages, both engineer-flagged: HotkeyStateMachine 87 code lines vs ~80 (17-line keycode data table; splitting it out would violate one-concern norm) and AppDelegate 21 vs ~15 (the R5 guard + launch logging). Accepted — overage is data/guard, not logic creep | accepted |
| R7 | 1A | Combo keycode map is ANSI-positional (kVK_ANSI_*): "z" in `hotkeyChord` means the physical ANSI-Z key, not the character z on the user's layout (AZERTY/Dvorak diverge). Fine for Phase 1 (no hotkey UI, D15); revisit when hotkey-config UI lands | open (future phase) |
| R8 | 1B | **BLOCKER — data race on `AudioRecorder.converter`.** `private var converter: AVAudioConverter?` is written on the main thread (`start()` assigns, `stop()` nils) and read on the audio render thread (`appendConverted`'s `guard let converter`), with no synchronization — the NSLock guards only `pcm`. `removeTap(onBus:)` does not synchronize with an in-flight tap callback, so `stop()`'s `converter = nil` (ARC release) can race the audio thread's load+retain → over-release/use-after-free. TSan-visible; rare crash in production. Surgical fix: capture the freshly created converter in the tap closure (`installTap { [weak self] buffer, _ in self?.appendConverted(buffer, using: converter) }`) and delete the `converter` stored property — no shared mutable state remains. The `pcm` path itself is correctly locked (post-stop stragglers are cleared by `pcm.removeAll()` at next start). **Fix applied and re-reviewed:** stored property deleted, converter is a local `let` captured immutably by the tap closure, `appendConverted(_:using:)` takes it as a parameter; stop()/catch no longer touch converter state, tap-removal-on-throw intact. Reviewer re-ran `make test`: 32/4 green. Race eliminated, no behavior change beyond the fix | resolved (1B) |
| R9 | 1B | Converter tail never flushed: `stop()` discards frames buffered inside AVAudioConverter (resampler filter delay, ~ms). Inaudible for dictation (user releases after speaking); a final `.endOfStream` drain would add complexity for no 1B value. No action | accepted |
| R10 | 1B | Temp WAVs (`LotusScribe-<UUID>.wav`) are written but never deleted — they accumulate in `temporaryDirectory` until OS purge. Spec §1B mandates the temp-file hand-off itself and 1C replaces the write with TranscriptionService, so this self-resolves; flagging so 1C review confirms the write path is actually removed. **1C review confirmed:** the write path is deleted from `DictationController.stopRecording()` (replaced by TranscriptionService call, not kept alongside); grep of `Sources/` for `FileManager` / `write(to` / `temporaryDirectory` / `.wav` shows zero file-write residue | resolved (1C) |
| R11 | 1C | In-flight transcription is not cancelled or coalesced on a new dictation: `stopRecording()` spawns an unstructured `Task` per key-up, so a user who starts a second dictation while the first transcribe is still in flight gets two concurrent requests completing in arbitrary order. Harmless in 1C (each just logs its transcript) and no retain issue (task captures the value-type service, not `self`). But 1D wires the same completion to *insertion* — a slow/stale transcript could paste after a newer one or into a different focused app. 1D must define ordering/cancellation (e.g. cancel or drop the stale task on the next `startRecording`) — **1D review confirmed D23 landed:** `generation` bumped at top of `startRecording()` (before the throwing `recorder.start()`, so a failed start still invalidates in-flight work); captured per-Task; checked *before* the empty-text guard; check→insert is synchronous MainActor code with no suspension point, so no interleaving window | resolved (1D, D23) |
| R12 | 1C | The "20 s timeout enforced" invariant (spec §1C) is set in code (`URLRequest(url:timeoutInterval: 20)`) but not guarded by any test — a one-line `#expect(request.timeoutInterval == 20)` in `requestMatchesSpec()` would make the invariant regression-proof (URLProtocol preserves `timeoutInterval` on the captured request). Minor; code verified correct by inspection. **1D review confirmed:** assertion present in `requestMatchesSpec()`, correctly placed on the URLProtocol-captured request | resolved (1D) |
| R13 | 1C | LoC: TranscriptionServiceTests.swift is 144 code lines vs the spec's ~90 estimate. Overage is the D19 `StubURLProtocol` + `bodyData(of:)` stream-drain infrastructure (~40 lines) the spec estimate under-scoped, not test-logic sprawl — R6 precedent applies. Accepted | accepted |
| R14 | 1C | `MultipartBody.data` is a non-mutating computed property (parts + fresh closing boundary each read), so parts can be appended after a read and every read is a complete valid body. Not a footgun: no finalize-then-mutate corruption is possible, and the only production caller builds and reads once inside `transcribe(wav:)`. No action | accepted |
| R15 | 1D | The transcribe `Task` now captures `self` strongly (1C deliberately captured only the value-type service; D23's generation read forces the self capture). Controller is app-lifetime, so no leak — worst case the Task extends nothing beyond the 20 s request timeout. Necessary and harmless | accepted |
| R16 | 1D | `TextInserter.insert` writes the pasteboard *before* creating the CGEvents, so a CGEvent-creation failure leaves the transcript on the clipboard with no paste. Consistent with the §1D clobber-accepted invariant and failure policy (nothing inserted); user can even paste manually. Note for the 1D HUMAN-AT-SCREEN failure matrix | accepted |
| R17 | 1E | StatusItemController delta is 24 insertions vs the spec's ~10 estimate. Overage is structural, not logic creep: NSMenuItem action dispatch requires an ObjC-visible target, forcing the NSObject subclass conversion (`override init` + `super.init()`) plus the lazily-built, retained `settingsWindowController` (spec's "reopening focuses it" behavior). R6 precedent applies | accepted |
| R18 | 1E | `SettingsForm.stored(_:)` maps only the exactly-empty string to nil — a whitespace-only or trailing-space value is saved verbatim (and the URL hint correctly fires, since URLComponents rejects it). Consistent with the §1E save-anyway policy; TranscriptionService already fails cleanly at request time on unparseable URLs. No trim added — would be silent input mutation the spec didn't ask for | accepted |
| R19 | 1E | SettingsForm reads SettingsStore once at init; an external `defaults write` while the pane is open isn't reflected, and the pane's stale field wins on next edit (last-writer-wins). Harmless for the bare 1E pane (external seeding was a pre-1E verify convenience); revisit only if a future phase adds live defaults observation | accepted |
| R20 | 1E hotfix | Nil-window fix verified: the explicit `convenience init()` (delegating to `init(store:)`) shadows the inherited `NSWindowController.init()` that Swift overload resolution preferred over the defaulted-arg candidate; the default on `init(store:)` was also removed, so the ambiguity can't silently return. Written without `override` — engineer empirically verified the compiler rejects `override` here (NSResponder designated-init quirk); the passing `parameterlessInitBuildsWindow` test proves the subclass init wins (inherited init would leave `window == nil` and fail the assertion). Fix is minimal: no call-site changes needed (StatusItemController's `SettingsWindowController()` now lands on the new init) | resolved (hotfix) |
| R21 | 1E hotfix | Zero-size fix verified: `window.setContentSize(420×300)` + root `.frame(width: 420, height: 300)`, with mirrored why-comments naming the coupling ("Must match SettingsForm's root frame"). Test correctly tightened from `frame.height > 0` — which PASSED against the live title-bar-only bug (~1×32 pt frame) — to `contentLayoutRect` ≥ 400×200: content-area assertions, not window-frame ones, are the load-bearing guard (code-norms "regression guards are tests"). The 420/300 literal pair lives in two files kept in sync by comment only; acceptable at two sites, extract a shared constant if a third appears | resolved (hotfix) |
| R22 | 1E hotfix | Permanent click-path logging (openSettings / show() entry / post-show window state) matches the established pattern exactly: `private static let logger = Logger(subsystem: "com.garisonlotus.LotusScribe", category: "<TypeName>")`, same as Permissions/TextInserter/etc. Info level with `.public` privacy on the post-show state line — appropriate for a user-facing "clicked Settings, nothing happened" diagnosis via Console streaming. Three log lines total; no noise risk | accepted |
| R23 | 1E hotfix | **Platform lesson for Phase 2 (pill overlay):** on macOS 26, `NSHostingController`'s fitting size reports ~0×0 for a SwiftUI grouped Form regardless of the root view's `.frame` (width-only `.frame` didn't take either). Any SwiftUI-hosted AppKit window — including Phase 2's NSPanel pill overlay — must size the window explicitly (`setContentSize` / explicit frame) and must not rely on `contentViewController` fitting-size autosizing; assert `contentLayoutRect`, not window frame, in its materialization test | open (Phase 2 guidance) |
| R24 | 1E follow-up | **Edit-menu fix approved.** `MainMenu.install()` builds an Edit-only main menu with nil-target items (verified: `addItem(withTitle:action:keyEquivalent:)` leaves target nil → responder-chain dispatch; test asserts it for Paste) and standard equivalents z/Z/x/c/v/a. No visual surface for an LSUIElement app — the menu exists purely as the key-equivalent dispatch route. Undo/Redo use stringly `Selector(("undo:"))`/`(("redo:"))` because AppKit exposes no typed anchor for them (they resolve via NSUndoManager on the responder chain) — correct, but they're the two items no test can cheaply cover; acceptable, HUMAN-AT-SCREEN covers Cmd-Z if it matters. `mainMenuRoutesPaste` relies on the xcodebuild test host having materialized `NSApp` — true today (other window tests already depend on it); if tests ever move to bare `swift test`, this test fails on nil `NSApp` first | resolved (1E follow-up) |
| R25 | 1E follow-up | **D26 save/cancel conversion approved.** Traceability: grep of `Sources/` shows the four `store.*=` setters exist *only* inside `SettingsDraft.save()` — save() is provably the sole write path; cancel()/titlebar close write nothing because drafts are local `@Published` state with no teardown hook (nothing to suppress). `show()` calls `draft.reload()` so reopen re-seeds (test covers external-change pickup too); D25 empty→nil moved intact into `SettingsDraft.stored(_:)` and now applies at Save time (tested: `llmEndpointURL "" → nil`). Tests drive `controller.save()`/`cancel()` — the exact methods the `.defaultAction`/`.cancelAction` buttons call, so button wiring is the only untested hop (HUMAN-AT-SCREEN verify-2 covers it). 300→350 height literal updated at both comment-coupled sites (R21: still two sites, still acceptable); test threshold 200→250 tracks it | resolved (1E follow-up) |
| R26 | 1E follow-up | R20 trap check on the designated-init conversion: `init(store:)` becoming designated (was convenience) actually *strengthens* the R20 fix — a Swift subclass that declares any designated init no longer inherits `NSWindowController.init()`, so the old silent-nil-window resolution path is now structurally impossible, not just shadowed. The explicit `convenience init()` and its `parameterlessInitBuildsWindow` guard test both remain; the test stays meaningful as the regression tripwire if someone reverts `init(store:)` to convenience-with-default. `required init?(coder:)` is `@available(*, unavailable)` + fatalError — correct for a code-only controller | resolved (1E follow-up) |

## Convention-violation tracking

| id | violation | files affected | resolution plan |
|----|-----------|----------------|-----------------|
|    | (none)    |                |                 |
