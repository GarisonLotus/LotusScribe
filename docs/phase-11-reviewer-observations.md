# Reviewer Observations — LotusScribe (Phase 11)

Numbered forward-looking items. id | item | status | sub-phase first raised.

| ID | Item | Status | Raised |
|----|------|--------|--------|
| R10A-2 | `keyName(for:)` does a linear `.first(where:)` scan of both maps per call. Fine for label rendering; revisit only if it ever lands in a per-frame path. (Carried from Phase 10; unrelated to mic selection.) | Open (inherited) | 10A |
| R10E2-1 | Onboarding Try-it hint/focus gating is IMPLICIT via the `stepIndex` switch, not a literal `stepIndex==3 &&` guard. Correct today; cheap to harden if `tryItStep` is ever reused. (Carried from Phase 10; unrelated to mic selection.) | Open (inherited) | 10E2 |
| R11A-1 | `defaultInputDevice()` calls `inputDevices()` a second full time (every device's UID/name/channel-count read again) just to map the default id → its entry. Harmless at menu-open cadence; revisit only if it ever lands in a hot path (e.g. per-frame or per-dictation). | Open | 11A |
| R11A-2 | `AudioInputMenuModel.defaultLabel` degrades to a bare "System Default" when the resolved name is nil/empty — an additive form not in spec §11A (which specified only "System Default (<name>)"); tested. Also handoff §3 uses "System Default (follow)" wording vs the spec's "(<name>)". Routed to architect for a copy round-trip. Non-blocking. | Open (→architect) | 11A |
| R11A-3 | `allDeviceIDs()` has a benign TOCTOU: device count can shrink between the size read and the data read, leaving trailing `0` (kAudioObjectUnknown) ids in the buffer. Safe today — those ids fail the downstream channel/UID/name reads and are `compactMap`-dropped — but worth a note if the read path is ever restructured. | Open | 11A |
| R11B-1 | D117 correctness rests on `input.inputFormat(forBus:0)` reflecting the just-pinned device synchronously after `setDeviceID`. This is the accepted AVAudioEngine pattern (deviceID set pre-start, format read after) and cannot be unit-verified (edge). It is therefore *exactly* the case the human-at-screen USB/external-mic check (spec §11B Verify) must exercise — pin a mic whose native rate differs from the built-in and confirm no `installTap` NSException. Non-blocking; the code ordering is correct. | Open (→human verify) | 11B |
| R11B-2 | `devices` is protocol-typed "so a test can inject a stub" (comment) but `AudioRecorder` exposes no initializer accepting it — the seam is not actually wired, so today the protocol typing buys nothing over the concrete type (mild YAGNI). Spec §11B line 107 prescribed this exact form ("injectable for future tests"), so non-blocking; flag only if the promised recorder-level test never materializes. | Open | 11B |
| R11C-1 | `menuNeedsUpdate` makes R11A-1's double-enumeration live at menu-open cadence: it calls `deviceEnumerator.inputDevices()` and `deviceEnumerator.defaultInputDevice()`, the latter re-running `inputDevices()` internally — so each submenu open enumerates all Core Audio input devices twice. Harmless at human-open cadence (not a hot path); noted only so the cost has a concrete call site. Non-blocking; behavior is correct. | Open | 11C |
| R11D-1 | `MicrophonePicker` declares a stored `private let enumerator = CoreAudioDeviceEnumerator()` (line 11) that is never read — `buildModel()` is `static` and constructs its own local enumerator (line 87). Dead property (YAGNI); either drop line 11 or make `buildModel()` an instance method that uses it. Non-blocking (no behavior/leak — a `CoreAudioDeviceEnumerator` holds no live resources), pure tidiness. | RESOLVED — dead property removed before the 11D commit (CLAUDE.md §3). | 11D |
| R11D-2 | `MicrophonePicker`'s in-menu `Label(…, systemImage: "checkmark")` is a UI idiom `HotkeyPicker` does not use (HotkeyPicker's `Menu` has no in-list checkmark). Correct rendering of `AudioInputMenuModel.isChecked`/`defaultIsChecked` here, but it is a deliberate divergence from the mirrored picker. Routed to architect for the UI-idiom call (per task boundary); not an execution defect. | RESOLVED — architect ruled KEEP (D118): cross-surface consistency with the 11C submenu + native SwiftUI Menu idiom. | 11D |
